# Runtime data — created by docker on first start.
/homeassistant/
/mosquitto/
/zigbee2mqtt/
/matter-server/
/matter-hub/

# Runtime config — copied from template.env, contains secrets after setup.sh.
/.env

# Local backups produced by sed -i.bak
*.bak